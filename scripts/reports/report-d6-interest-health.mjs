/** D6: Interest calibration health */import{getSupabase,runQuery,mdH2,mdTable,pct,registerReport}from"./lib/report-utils.js";registerReport({id:"d6",suite:"D",title:"Interest Calibration Health",etaSeconds:2,async run(){const sb=getSupabase();let md=mdH2("D6: Interest Calibration Health");const{data:rows}=await runQuery("Interest scores",()=>sb.from("user_interest_scores").select("calibrated_weight").limit(100000),2);if(!rows)return md+"_No data._\n";const b=[{label:"<0.5 (cold)",max:0.5},{label:"0.5-1.0 (warm)",max:1.0},{label:"1.0-2.0 (hot)",max:2.0},{label:"2.0+ (very hot)",max:Infinity}];for(const x of b)x.c=0;for(const r of rows){const w=r.calibrated_weight||0;for(const x of b){if(w<=x.max){x.c++;break;}}}const t=rows.length;md+=mdTable(b,[{key:"label",label:"Range"},{key:"c",label:"Scores",align:"right",format:v=>v.toLocaleString()},{key:"pct",label:"%",align:"right",format:(v,r)=>pct(r.c,t)}]);md+=`${t.toLocaleString()} total scores | Active (>1.0): ${rows.filter(r=>(r.calibrated_weight||0)>1).length.toLocaleString()}\n`;return md;}});