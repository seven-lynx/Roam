/** E4: Streak health */import{getSupabase,runQuery,mdH2,mdSummaryCards,registerReport}from"./lib/report-utils.js";registerReport({id:"e4",suite:"E",title:"Streak Health",etaSeconds:2,async run(){const sb=getSupabase();let md=mdH2("E4: Streak Health");const{data:rows}=await runQuery("Profiles",()=>sb.from("profiles").select("current_streak,longest_streak").limit(100000),2);if(!rows)return md+"_No data._\n";let active=0,total=0,maxStreak=0;const dist=new Map();for(const r of rows){total++;const s=r.current_streak||0;if(s>0)active++;if(s>maxStreak)maxStreak=s;dist.set(s,(dist.get(s)||0)+1);}md+=mdSummaryCards([{label:"Total Users",value:String(total)},{label:"Active Streaks",value:String(active)},{label:"Max Streak",value:String(maxStreak)}]);return md;}});