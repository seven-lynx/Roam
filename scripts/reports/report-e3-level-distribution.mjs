/** E3: Level distribution */import{getSupabase,runQuery,mdH2,mdTable,pct,registerReport}from"./lib/report-utils.js";registerReport({id:"e3",suite:"E",title:"Level Distribution",etaSeconds:2,async run(){const sb=getSupabase();let md=mdH2("E3: Level Distribution");const{data:rows}=await runQuery("Profiles",()=>sb.from("profiles").select("level").limit(100000),2);if(!rows)return md+"_No data._\n";const m=new Map();for(const r of rows){const l=r.level||1;m.set(l,(m.get(l)||0)+1);}const t=[...m.entries()].map(([level,count])=>({level,count})).sort((a,b)=>a.level-b.level);md+=mdTable(t,[{key:"level",label:"Level",align:"right"},{key:"count",label:"Users",align:"right",format:v=>v.toLocaleString()}]);return md;}});