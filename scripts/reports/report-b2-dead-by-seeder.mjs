/** B2: Dead by seeder */import{getSupabase,runQuery,mdH2,mdTable,pct,registerReport}from"./lib/report-utils.js";registerReport({id:"b2",suite:"B",title:"Dead URLs by Seeder",etaSeconds:3,async run(){const sb=getSupabase();let md=mdH2("B2: Dead URLs by Seeder");const{data:rows}=await runQuery("Dead by seeder",()=>sb.from("urls").select("source,inactive").not("source","is",null).limit(200000),4);if(!rows)return md+"_No data._";const m=new Map();for(const r of rows){const s=r.source||"?";let e=m.get(s);if(!e)e={source:s,total:0,dead:0};m.set(s,{source:e.source,total:e.total+1,dead:e.dead+(r.inactive?1:0)});}const t=[...m.values()].sort((a,b)=>b.total-a.total);md+=mdTable(t,[{key:"source",label:"Source"},{key:"total",label:"Total",align:"right",format:v=>v.toLocaleString()},{key:"dead",label:"Dead",align:"right",format:v=>v.toLocaleString()},{key:"pct",label:"Dead %",align:"right",format:(v,r)=>pct(r.dead,r.total)}]);return md;}});